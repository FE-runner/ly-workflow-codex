## 1. codex/reviewer.md、plan-reviewer.md 输出契约修正

- [x] 1.1 把 `templates/prompts/codex/reviewer.md` 的 "Scoring Format"（VALIDATION REPORT / XX/100 打分）替换为与 `plan-reviewer.md` 一致的 Critical/Warning/Info 分级输出结构，包含"无发现时明确写'未发现问题'"的说明。
- [x] 1.2 在 `templates/prompts/codex/reviewer.md` 与 `templates/prompts/codex/plan-reviewer.md` 的 Response Structure 里都补充路径契约：每条 Critical/Warning/Info 的"位置"字段必须给出至少一个相对 `WORKDIR` 的可解析文件路径；跨文件/范围性问题（不存在单一目标文件）必须列出全部相关文件路径，不能只给章节名。核验 `plan-reviewer.md` 现有的 `[文件:章节/条目]` 格式是否已满足，不满足则一并修改。

## 2. review-code.md：首轮 TASK 改为传路径不传全文

- [x] 2.1 步骤 1（判定审查范围）逻辑保持不变，只调整步骤 2 的 Bash/TASK 构造：不再由 Claude 读取 `git diff`/未跟踪文件内容拼进 TASK，改为传递"基线引用说明（如 `git diff HEAD`/`git diff HEAD~1`/`git show HEAD`/零 commit 场景下的 `git diff --cached` + `git diff` 命令组合说明）+ 未跟踪文件路径清单"，并指示 Codex 自行在 `WORKDIR` 下执行对应命令/读取文件获取内容。删除原步骤 1 里"构造一份稳定快照"的措辞，改为固定命令组合说明。
- [x] 2.2 更新步骤 2 的失败判定说明：确认"审查调用失败"判定逻辑不受 TASK 内容变化影响（仍按返回内容能否解析为 Critical/Warning/Info 判定）。

## 3. review-code.md：第 2 轮起改为增量传递

- [x] 3.1 新增/修改步骤 3（审查-修复循环）说明：第 2 轮起，TASK 内容 = "上一轮全部 Critical 原文（逐字）" + "路径清单（本轮实际改动的文件 ∪ 上一轮全部 Critical 各自指向的文件, 即使未被修改)"，不再重新构造"基线 → 当前完整状态"的整体差异文本。
- [x] 3.2 明确"路径清单之外的文件不重新整段传入"这一约束在模板文字中落地，避免退化回全量重传；同时检索并替换 review-code.md 里残留的"复审基线→当前工作区完整状态"等与增量传递矛盾的旧措辞。
- [x] 3.3 保留现有的"熔断/分歧未决"判同键逻辑（文件路径+问题类别+定位锚点）不变，确认增量传递不影响这一判定所需的信息（Critical 原文仍完整逐字传递，且判定所需文件路径已在路径清单中）。
- [x] 3.4 明确"本轮实际改动文件清单"在修复步骤中即时记录，不得在后续轮次靠"当前 git 状态"反推——循环期间不逐轮 commit，但后续轮次会继续修改文件、用户可能对同一文件有原有改动、文件也可能被删除/重命名，仅凭某个时间点的 git 状态无法可靠还原"本轮具体改了什么"；命令模板要求在 3.2 修复步骤完成时就把改动文件路径写入本轮报告，第 3.5 步触发下一轮时直接复用该清单构造路径清单。
- [x] 3.5 明确 Critical 位置字段缺失可解析路径时的兜底处理：`/ly:review-code` 场景下, Codex 返回内容若因缺少路径导致无法可靠解析，按现有"审查调用失败"终止条件处理（而不是静默丢弃该 Critical），对应 spec.md 中的说明。
- [x] 3.6 增量路径清单需处理文件删除/重命名场景：若上一轮某条 Critical 指向的文件在本轮修复中被删除或重命名，路径清单改用新路径（若有）并在 TASK 中说明该文件的状态变化，避免 Codex 尝试读取不存在的旧路径或被误判为审查调用失败。

## 4. review-plan.md：首轮与第 2 轮起的 TASK 调整

- [x] 4.1 步骤 2（读取工件）保持"枚举 proposal/design/tasks/specs 路径"逻辑不变；步骤 3（调用 Codex）TASK 改为只传该 change 目录路径 + 枚举到的各文件相对路径清单，不再由 Claude 读取全文拼接。
- [x] 4.2 第 2 轮起（步骤 4.5 触发的复审），TASK 改为"上一轮全部 Critical 原文（逐字）+ 路径清单（本轮实际改动的文件 ∪ 上一轮全部 Critical 各自指向的 artifact/delta spec 文件, 即使未被修改)"，不再重新传整份 proposal/design/tasks/specs 内容；同时检索并替换 review-plan.md 里残留的"每一轮都重新读取全部工件当前内容"这类与增量传递矛盾的旧措辞（仅首轮适用该措辞）。
- [x] 4.3 确认 delta spec 文件路径清单在首轮和后续轮次中都完整列出（不能只写"读取 specs 目录"这种模糊指代），避免 Codex 漏读部分 delta spec。
- [x] 4.4 同 3.4：本轮实际改动的 artifact/delta spec 文件清单在 4.2 修复步骤完成时即时记录并写入本轮报告，供 4.5 触发的下一轮直接复用，不依赖运行时的 git 状态反推。
- [x] 4.5 明确 Critical 位置字段缺失可解析路径时的兜底处理：将该 change 目录下全部 artifact/delta spec 路径保守纳入下一轮路径清单，并在报告中说明该情况（对应 spec.md 新增 Scenario）。
- [x] 4.6 步骤 2 增加检测：若某份 delta spec 文件文字引用了基线 spec 中未被本次修改的既有 Requirement，额外把对应基线 `openspec/specs/<capability>/spec.md` 路径纳入首轮路径清单，并在 TASK 中说明该文件仅作审查上下文、不属于修复对象。
- [x] 4.7 增量路径清单需处理文件删除/重命名场景：若上一轮某条 Critical 指向的文件已被删除或重命名，路径清单改用新路径（若有）并在 TASK 中说明该文件的状态变化，不让 Codex 尝试读取不存在的旧路径。
- [x] 4.8 `codex/plan-reviewer.md` 的 Review Checklist 补充区分"proposal 未声明任何 capability（无 delta spec 正常）"与"proposal 声明了 capability 但无任何 delta spec（报 Critical）"两种情形的判断依据，避免继续沿用"没有 delta spec 就整体容错跳过"这一条被误用到后一种情形。

## 5. 报告模板：逐轮展示 Codex 原始发现

- [x] 5.1 review-code.md 步骤 3（循环体）内新增"本轮执行日志"结构，包含"Codex 本轮原始发现（逐字）"区块与"Claude 认可/不认可判定"区块并排展示，每一轮都产出一份、不合并——包含首轮 Critical 为 0、直接跳到步骤 4 结束的情况，该轮 Codex 返回的原始内容也必须以同样结构展示，不因为没有进入循环体就被跳过；步骤 4（最终报告）是独立于逐轮执行日志的汇总，只汇总最后一轮的 Warning/Info 与全部轮次的已修复 Critical 摘要，不是"沿用逐轮结构只展示最后一轮"——模板文字需明确区分这两种展示载体，避免被理解为中间轮次的原文只保留在最终报告里。
- [x] 5.2 review-plan.md 对应步骤 3（首轮无 Critical 直接结束的情况）与步骤 4（循环体）/步骤 5（最终报告）做同样调整，同样区分"逐轮执行日志"与"最终汇总报告"两种载体，且首轮无 Critical 时也要展示该轮原始发现。
- [x] 5.3 确认现有"分歧未决"终止场景的"并列展示两轮原文+反驳理由"要求与本次"每轮都展示"的新要求不冲突（新要求是老要求的超集，老要求的展示动作在每一轮都发生，终止时只是多轮堆叠展示）。
- [x] 5.4 模板文字明确"逐轮展示"是指"该轮 Codex 调用返回后、报告中呈现"，不是 Codex 进程执行期间的流式展示，避免被误读为承诺实时输出。
- [x] 5.5 最终报告的 Critical 摘要改为用非技术人员能看懂的人话概括问题和已做改动，逐字 Codex 原文区块作为补充材料并存，不作为报告主体。

## 6. 校验

- [x] 6.1 `openspec validate --changes optimize-review-loop-incremental-context` 通过。
- [x] 6.2 人工走读 review-code.md/review-plan.md 全文，确认改动后步骤描述内部一致（无残留的"重新读取全文"/"整体差异"等与增量传递矛盾的措辞）。

## 7. 提交行为改造：循环期间不提交，清零后统一提交一次

- [x] 7.1 review-code.md 步骤 3（原"3.4 提交本轮改动"）改为：每轮修复+验证通过后不再执行 commit，只把本轮实际改动的文件清单记录进本轮报告（供第 3.5 步下一轮增量传递使用）；步骤 4（最终报告）在"正常清零"分支下新增一步：对循环全程所有轮次实际改动的文件执行一次统一 commit，提交信息形如 `fix: review-code (经 N 轮修复)`；非清零终止分支（熔断/无法安全修复/验证失败/分歧未决/审查对象类型持续系统性误判/达到轮数上限）不执行任何 commit。
- [x] 7.2 review-plan.md 步骤 4（原"4.4 提交本轮改动"）做同样调整：循环期间不提交，步骤 5（最终报告）在"正常清零"分支下统一提交一次，提交信息形如 `fix: review-plan feedback (经 N 轮修复) - <change-name>`；非清零终止分支不提交。
- [x] 7.3 两个命令都保留 `--no-commit` 标志，语义改为"连清零后的这次最终统一提交也不做"。
- [x] 7.4 更新终止条件列表：移除"提交失败"作为循环中途的终止条件项，改为清零分支下"统一提交失败"的独立说明（提交失败时不重新进入循环，因为已经清零，只是需要人工手动完成这次提交）。
- [x] 7.5 `templates/commands/propose.md` 里"（默认逐轮自动 commit……）"相关描述同步改为"循环期间不提交，清零后统一提交一次"，确认编排逻辑（例如 6a.1/6b.3 对 `/ly:review-plan` 的调用说明）不依赖"逐轮提交"这一已废弃行为。
- [x] 7.6 review-plan.md 步骤 1（解析目标 change）之后新增一步：对修复对象文件（`proposal.md`/`design.md`/`tasks.md`/delta spec）逐一执行 `git status --porcelain` 记录循环开始前的干净/脏状态；步骤 5（最终报告）统一提交时，跳过循环开始前已脏的文件，并在报告中单独列出这些被跳过的文件及原因。review-code.md 不需要这一步（见 spec.md 中的说明：其审查对象本身就是未提交变更，不存在"意外混入"问题）。
